if SHIELD_CENTRAL
    config ZMK_KEYBOARD_NAME
        default "MeKaBu"

    config ZMK_SPLIT
        default y

    config ZMK_SPLIT_ROLE_CENTRAL
        default y
    
    config ZMK_SPLIT_BLE_CENTRAL_PERIPHERALS
        default 1
endif

if SHIELD_MKB_L || SHIELD_MKB_R
    config ZMK_SPLIT
        default y
endif 