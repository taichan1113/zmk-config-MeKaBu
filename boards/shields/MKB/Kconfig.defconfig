if SHIELD_MKB_R
    config ZMK_KEYBOARD_NAME
        default "MeKaBu"

    config ZMK_SPLIT
        default y

    config ZMK_SPLIT_ROLE_CENTRAL
        default y
    
endif

if SHIELD_MKB_L
    config ZMK_KEYBOARD_NAME
        default "MeKaBu"

    config ZMK_SPLIT
        default y
endif 